힌트를 보면 다른 값을 통해 오직 비교를 한다고 한다.
아마 제목이 md5_compare인 것을 보니 md5해싱된 값을 비교하는 형태의 문제일 것 같다.

문제에 접속해보니 두 개의 값을 입력할 수 있게 되어있다.

소스를 보니 첫번째 입력값은 문자인지 두번째 입력값은 숫자인지 필터링을 하고 있으며
그 후 둘다 md5 해싱 후 값을 비교하여 같으면 문제가 해결되는 것 같다.

웹서핑을 해보니 md5 취약점중 매직해시라는 취약점이 있는데 ==나 != 비교연산자로 비교시 
두 변수의 값이 0x<숫자>의 형태를 띄고 있으면 Type Juggling(자동으로 타입을 설정해주는 기능)을 통해
두 변수가 모두으로 인식되어 같은 것으로 인식하는 취약점이 존재하는 것을 발견했다.

마침 우리가 풀고 있는 문제와 딱맞는 예제로 
240610708 , QNKCDZO 이 두 값 모두 md5해싱시 0x<숫자> 형태의 값을 띈다.

값을 넣어보니...

클리어!!
