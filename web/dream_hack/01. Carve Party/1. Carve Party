문제에 접속해보니 호박하나 모양의 캔버스 하나가 주어져있고 10000번을 누르라고 되어있다.

아마도 10000번을 눌러야 flag값이 나오는 것 같은데

소스를 보니 counter라는 변수를 이용하여 몇번을 눌렀는지 확인하는 것을 알 수 있었다.

counter 값을 10000으로 바꾸고 클릭해보니 하단에 이상한 문자열이 나타난다.

이렇게 간단하게 해결인가? 했지만 아쉽게도 이것은 flag가 아니였다.

나타난 문자열이 느낌이 뭔가 너무 이상해서 소스를 다시보니 한번에 counter 값을 늘릴경우 
flag값이 아닌 이상한 값이 출력된다는 사실을 알게 되었다.

따라서 확실하게 문제를 해결하기 위해서는 한번에 값을 올리는 방법이 아니라 1씩 inc를 해야한다는 것인데 그래서 document.getElementById('').click()으로 for문을 이용하여 10000번을 클릭하려고 하니 막아놨는지 안된다.....

소스를 다시 보던중 jquery를 이용한 click() 메서드를 발견하였고 이를 이용하여 for문으로 10000번을 눌러보니 제대로된 flag값이 떴다!!!!

그러나 문제는 이것은 canvas로 그린 것이기 때문에 문자열을 복사 할 수가 없다는 것이였다....

다시 소스를 보니 txt라는 변수를 이용해 값을 저장하고 이 문자값을 토대로 canvas로 그리는 것을 알 수 있었고 console.log(txt); 를 소스 중간에 추가하고 다시 10000번을 for문으로 클릭해보니
console에 flag값이 올라왔다!!

클리어!!
