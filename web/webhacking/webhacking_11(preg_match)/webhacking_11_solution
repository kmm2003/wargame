php 소스코드를 분석해보면
val값을 정규표현식을 이용하여 매칭하고 매칭에 성공하면 문제가 클리어되는 것을 확인 할 수 있다.

정규표현식 pat은 
1~3중 하나 + [a~f중 하나를 5번] + '_' +[단일문자를 최소0번이상쓰고] + [클라이언트 pc ip주소]
+ [단일문자를 최소0번이상쓰고] + tab + 'p' + tab + 'a' + tab + 's' +tab + 's' 의
형태로 선언되어있다. 이 조건을 만족시는 val 값을 넣어 get방식으로 전송하면 문제가 해결되겠다.
val에 1abcde_[pc ip주소]%09p%09a%09s%09s를 넣어 보냈다.
==> 여기서 tab을 %09로 쓴 이유는 get방식으로 전달시 tab은 %09로 이스케이프 되어 표현되기 때문이다.

결과는 클리어!!

