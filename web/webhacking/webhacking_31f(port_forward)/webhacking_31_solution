31번 문제에 접속하면

두 줄의 php코드와 함께 에러메시지 창이 뜨는 것을 확인할 수있으며 index.php의 23번 라인에 오류가
발생 했음을 알려주고 있다. 아마도 위의 두 줄의 php코드가 23번라인대임을 짐작해볼 수 있다.

코드를 해석해보면 포트값을 10000~10100 사이의 값으로 랜덤하게 주고있으며
fsockopen()함수를 이용해 {연결할 서버 ip, port, 에러넘버, 에러시 출력할 스트링,연결시도제한시간}
이렇게 5가지를 인자로 넣어 소켓을 열고 있다.

아마도 이렇게 소켓을 열었지만 에러가 생겨 에러시 출력할 스트링이 출력됬음을 알 수 있다.

die()의 경우 완전히 이상한 server값이 들어가면 출력되는 것을 여러 값을 넣어보며 확인했다.

우선 가장먼저 생각난 것은 100개의 포트중 랜덤해서 포트값이 들어가고있으며 포트값이 잘못된 경우
에러를 발생시키는 것이 아닐까?? 추측해본다. 따라서 지속적으로 페이지에 request를 보내어 간접 port 스캐닝을 시도해보아야겠다.

사실 포트스캐닝도 하나의 방법이겠지만 더 정확한 방법은 포트포워딩을 이용하는 방법이다.

포트포워딩을 이용해 게이트웨이에서 특정포트 범위에서 포트가 열리는 경우 그것을 하나의 포트로 열리게끔 통일 시켜주는 작업을 할 수 있기 때문이다.

하지만 여기서 이것을 시행할 수는 없기 때문에 패스
