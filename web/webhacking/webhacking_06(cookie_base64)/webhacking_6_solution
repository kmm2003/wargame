id와 pw값과 함께 소스코드를 볼 수 있게 되어있다.
소스코드를 먼저 봐보자

소스코드를 해석해보면

id와 pw값을 base64 인코딩 후 각각의 값들을 여러번 replace 한 값을 
각각 user,password 쿠키에 값으로 넣었다

그리고 각 쿠키의 값을 다시 원래대로 위에서 replace했을때와 반대로 replace 후 base64 디코딩을
하여 decode_id와 decode_pw에 각각의 값을 저장한다.

이때 decode_id의 값이 admin 이고 decode_pw의 값이 nimda이면 이 문제는 해결되게된다

이 문제를 보면 쿠키에 저장된 암호문의 평문이 guest와 123we임을 알 수 있고 최종적으로 디코딩 된 값도 맨처음에 본 id와pw값을 통해 똑같은 값임을 알 수 있다. 이러한 점을 이용하여
어짜피 디코딩된 결과가 admin,nimda로 나오면 되는 것이기 때문에 반대로 python을 이용하여
admin과 nimda를 인코딩한 값을 우리가 쿠키값으로 넣어주면 문제가 해결될 것이다.
따라서 python을 이용하여 인코딩하겠다.

인코딩후 각각을 각각의 쿠키값에 넣은 후 페이지를 리로드 해보았다.

클리어!!

