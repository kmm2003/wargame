문제에 접속하면 뜬금없이 디버깅을 해보라는 문구가 나온다.

개발자 도구를 이용해 소스를 보면 스크립트 태그에 난독화된 자바스크립트 코드가 나온다.

자바스크립트 난독화 해제 사이트를 이용하여 난독화를 해제해 보았다.(난독화해제 스크립트)

난독화를 해제하고 봐도 소스코드양이 많고 복잡해서 생각을 하다가 처음에 페이지를 실행하면 debug me라는 문구가 alert창을 통해 뜨는 것을 확인했었고 이를 계기로 소스코드에 alert를 검색해보았더니 한개만 나왔다 즉, 여기서 해결을 해야하는 것이라고 생각이 드는데 alert문이 뜨는 조건인 if문을 보면 

location[b('0x19', 'iUmC')][b('0x1a', '6]r1')](0x1) == b('0x1b', 'RLUb')

위와 같은 조건문이 나왔고 위의 조건문이 true여야 문제가 해결되는 것 같다. (왜냐하면 false일때
alert명령이 실행되기 때문이다.)

b라는 함수 위 조건문에서 여러번 실행되기 때문에 b함수를 찾아보았다.

b함수를 찾아보니 여러과정을 거쳐서 리턴값을 뽑아내는 과정인데 좀 복잡하게 구성되어있다. (암호화 과정인듯?)

근데 여기서 문득 떠오른 방법이 b()는 함수고 따라서 b()함수와 인자를 console창에 넣으면 리턴값이 출력될 것이다. 따라서
위의 if문의 b함수 3가지의 리턴값을 모두 구해보겠다.

location["hash"]["slice"](0x1) == "Passw0RRdd"

위와 같은 값으로 변환되며 0x1은 아스키코드로 해더의 시작??? 을 의미한다는데 솔직히 정확하게 무슨의미인지 파악이 안된다.
확실한 것은 slice함수의 인자로 사용될 것이라는 것이다.

위를 더 알아보기 쉽게 dom의 형태로 풀어쓰면 다음과 같다.

location.hash.slice(0x1) == "Passw0RRdd"

즉 loaction.hash.slice(0x1)을 한 값이 "Passw0RRdd"이면 문제가 해결된다.

현재의 loaction.hash의 값을 확인해보니 공백이 들어가있다. "Passw0RRdd"를 넣어서 loaction.hash.slice(0x1)값을 확인해
보겠다.

확인해보니 loaction.hash.slice(0x1) == "Passw0RRdd" 으로 원하는 값이 나왔다!! 디버깅을 시도해보겠다.

흠....변화가 딱히 없다.....

그래서 if문이 true일때 무슨 코드가 실행되는지 확인해보았다.

location[b('0x1c', '4c%d')] = b('0x1d', 'llaF');
==> location.href="./?Passw0RRdd=1"

위와 같은 코드가 실행된다. 즉, 페이지의 url이 위와같은 경로로 변경되는 것을 확인할 수 있다. 직접 들어가보자.

클리어!!










