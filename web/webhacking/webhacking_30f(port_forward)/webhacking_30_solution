30번 문제에 접속하면 나의 개인적인 디렉토리?는 ./upload/eBSmkKdVBld5
디렉토리에 존재하며 저 디렉토리안에 있는 소스코드 url을 띄워주고 있다.

파일을 하나 업로드하여 확인해보겠다.

파일을 하나 업로드해보니 정상적으로 예상 경로에 업로드 되면서 URL경로까지 하이퍼링크로 표시해준다.

소스코드를 한번 살펴보자

mysql DB에 연결 후 연결한 데이터베이스를 chall30으로 변경한다.
그 후 chall30 DB에 있는 chall30_answer 테이블에서 flag값을 select하여 $result 변수에 저장하고 result[0] 즉 ,flag값이 존재할 때 문제가 해결되는 것 같다.

mysqli_connect()를 통해 DB랑 연결하는데 이때 인자값이 없음으로 디폴트값이 들어간다.
여기서 중요한 것은 디폴드값의 경우 php마다 기본적으로 설정되어있는 값이 있고 이를 조작하여 나의 데이터베이스에 chall30_answer이라는 테이블을 똑같이만들어서 접속하면 해결되는 문제다.

패스~


