54번 문제에 접속하니 password is ? 라는 문구가 시작으로 ?값이 여러값으로 순식간에 바꼈다가 사라지는 모습을 확인할 수 있다. 여기서 나는 한번에 flag라는 문자가 지나가는 것을 보았고 이를 통해 자바스크립트를 조작하여 flag값을 알아내는 문제라고 단번에 직감했다.

개발자도구를 통해 소스코드를 보았으며 
aview.innerHTML=x.responseText; 
위 부분을 통해 ?부분 값이 연속적으로 바뀌는 것임을 파악하였다.

따라서 aview.innerHTML+=x.responseText;으로 코드를 변경후 console창에 스크립트 소스자체를 다시 정의해주었다 그러니 flag값이 전부다 나온다 ㅎㅎ

클리어!
