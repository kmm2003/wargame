가장 먼저 보이는 문구가 자바스크립트 챌린지라는 문구가 보이고 이걸 본 순간 나는 개발자도구를 바로
켰다.

개발자도구에서 스크립트 태그를 확인해 보니 무지막지하게 알아보기 힘들게 인코딩이 된 소스코드를 발견할 수있었다.

이것을 콘솔창에 입력해서 소스코드를 확인해 보아야 할 것 같다. 근데 개발자도구로 소스코드를 확인 했을때 소스코드의 양이 너무 많은 나머지 모든 값이 보이지 않는다 따라서 소스탭에 들어가서
풀 소스코드를 복사하여 콘솔창에 입력해 보겠다.

띠용??? 정의가 되지 않았다만 뜬다.....허허;;

도저히 모르겠어서 풀이를 보았는데 맨뒤의 소스코드를 빼주면 소스코드가 나온다.....
(아무리 뒤져봐도 왜 맨 뒤에꺼를 빼준 것인지 모르겠다.... 모든 풀이들이 그냥 빼준다 이런식임;;)

우선 그렇다치고 빼고 콘솔에 넣어보면 소스코드가 나온다.

소스코드가 전부 안보이는 경우가 생기는데 소스코드를 더블클릭하면 소스탭으로 넘어가면서 소스코드가 모두 보이게 된다. 확인해보자

소스코드를 확인해보면 String.fromcharcode()를 이용하여 보기 힘들게 난독화 시켜놓았는데
fromcharcode()는 숫자로 표현된 값을 ascii 코드표에 해당되는 문자로 변환시켜주는 함수이다.
즉, sql로 따지면 char()함수랑 같은 역할이다.

첫번째 for문을 보면 fromcharcode()를 122번이나 이용하여 enco변수에 값을 넣어주었다. 나는 이 부분만 따로 떼어내서 콘솔창에 넣어 enco값을 확인했다.
enco값을 확인해보니 모든 특수문자,숫자,영문자(대/소) 등이 모두 출력되었다.
아마 enco를 이용하여 문자나 숫자를 뽑아내려한다고 유추해볼 수 있다.

아래쪽에 보면 charCodeAt(i)함수가 있다. 이것은 특정 문자열안에서 i번째 인덱스에 있는 값의
아스키코드 값(10진수값)을 출력해주는 함수이다.

소스코드를 보면 이것을 이용하여 ck와 비교를 해주고있는데 l이라는 변수를 선언하여 비교하는 값을
콘솔창에 소스를 작성하여 확인해 보도록 하겠다.

ck와 비교하는 값은 =youaregod~~~~~~~!라는 문자열임을 확인 할 수 있었다.

소스코드를 다시 확인해보면 ck와 =youaregod~~~~~~~!을 비교하여 같으면 문제가 해결되는 것으로
보인다.

location.href="./"+ck.replace("=","")+".php";

위 코드가 비교하여 같으면 실행되는 코드이고 이것을 해석하여 직접 페이지로 이동해 보겠다.
해석하면

'./'+'youaregod~~~~~~~!'+'.php' == ./youaregod~~~~~~~!.php 로 해석된다. 
페이지를 이동해보겠다.

클리어!!

