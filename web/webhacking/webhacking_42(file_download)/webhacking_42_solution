txt, docs 두가지의 파일을 다운로드 할 수 있는데 txt파일은 다운로드가 되지만 docx파일은
접근이 금지된다고 뜬다.

docx파일의 이름이 flag인 것을 보니 이 파일에 접근하여야 문제가 해결되는 것 같다.

개발자도구로 소스코드를 봐보니 down이라는 파라미터값을 통해 파일이 다운로드 되는 것 같다.

이때 txt파일의 down파라미터 값을 보니 dGVzdC50eHQ= 의 형태이며 맨 마지막값이 =인 것을 보아 
base64인코딩 된 값이라고 추측해볼수있다. 한번 디코딩해보자

디코딩해보니 test.txt 이라는 값이 나온다!

그렇다고 down파라미터의 값으로 flag.docx를 base64인코딩한 값을 넣어서 url에 접속하면 다운로드가 될 것이다. 해보자

flag.docx ==> ZmxhZy5kb2N4

?down=ZmxhZy5kb2N4으로 접속해보니 역시나 다운로드가 된다. 
파일을 열어보니 flag값이 있다!! 

클리어~!!


