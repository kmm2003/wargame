소스코드를 살펴보면 unlock이라는 변수에 복잡한 계산값이 들어가는 것을 알 수 있으며
14번 문제와 비슷하게 form에 입력값과 unlock의 입력값이 같으면 클리어되는 것 같다.

복잡한 계산값을 해결해야하는데 콘솔창에 넣으면 바로 브라우저가 알아서 계산해주기 때문에 계산해보았다.

unlock에는 7809297.1이라는 값이 들어가는 것을 알 수 있고 이값을 그대로 복사하여 폼에 넣고 체크
버튼을 눌렀다.

클리어!!


