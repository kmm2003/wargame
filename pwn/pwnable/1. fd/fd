문제에 접속해보니 fd.c 라는 파일이 보인다.

소스를 보니 인자를 2개이상 넣어야 하며 
argv[1]의 값을 atoi로 정수형으로 변환 후 0x1234를 뺀 값을 fd에 넣고 있다.

그 후 read함수를 이용해 fd로 가리키는 파일의 값을 읽어와 buf에 저장 후 "LETMEWIN" 이라는 값과
비교하여 같으면 flag를 반환해준다.

우선 문제를 해결하기 위해서는 read 함수의 fd인자에 대해 조사해봐야될 것 같다.

조사해보니 fd인자는 파일을 가리키는 인자인데 
기본적으로 0이면 표준입력, 1이면 표준출력, 2이면 표준에러를 의미한다.

따라서 fd인자값을 0으로 만들어서 직접입력하여 buf의 값을 "LETMEWIN"으로 만들면 문제가 해결 될 것이다.

우선 상단코드에서 argv[1]값에서 0x1234를 빼주기 때문에 argv[1]을 0x1234로 만들어야 하는데
0x1234는 16진수이기 때문에 atoi로 반환되는 입력값을 0x1234로 넣을 수는 없고 0x1234를 10진수로 변환 후 그 값을 넣어주면 되겠다.

0x1234를 10진수로 변환하면 4660이므로
./fd 4660
LETMEWIN
이라고 차례대로 입력하면??

클리어!! 

